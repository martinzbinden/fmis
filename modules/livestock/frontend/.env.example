# Leer lassen für Produktion (Frontend + Backend laufen same-origin hinter
# Traefik unter derselben Domain, relative Pfade reichen).
# Für lokale Entwicklung (separater Vite-Dev-Server + Backend auf :8000):
# VITE_API_URL=http://localhost:8000
VITE_API_URL=
